# Admin API config
PORT=8787
# Permitir uno o varios orígenes del frontend (coma separada)
# Ejemplos:
# FRONTEND_ORIGINS=http://localhost:5173,http://localhost:3000
# o el anterior FRONTEND_ORIGIN para un solo origen
FRONTEND_ORIGINS=http://localhost:5173

# Supabase Service Role credentials
SUPABASE_URL=https://YOUR_PROJECT.supabase.co
SUPABASE_SERVICE_ROLE_KEY=YOUR_SERVICE_ROLE_KEY

# Optional simple token gate; if unset, endpoints are open (no auth)
ADMIN_API_TOKEN=change-me-strong-token